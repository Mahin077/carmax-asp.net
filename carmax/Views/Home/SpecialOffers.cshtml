@model IEnumerable<carmax.Models.product>
@{
    ViewBag.Title = "SpecialOffers";
}

<head>
    <link rel="stylesheet" href="~/Content/Homepage.css">
</head>
<div class="container">
    <div style="margin-top:100px ;" class="row">

        @foreach (var item in Model)
        {
            <div class="col-lg-4 col-md-6 col-sm-12 mb-5">

                <form action="#" method="GET">

                    <div class="card " style="width: 18rem;margin:auto;">
                        <img src="@Url.Content(item.img)" class="card-img-top" alt="..." height="200px">
                        <div class="card-body mt-5">
                            <h5 class="card-title">@item.name</h5>
                            <p class="card-text" style="font-weight: bold;">
                                Price :<span style="font-weight: bold; text-decoration: line-through;">&#2547; @item.price</span>
                                <span>(@item.discount%off)</span>
                            </p>

                            <p class="card-text" style="font-weight: bold;">New Price : <span style="color: red">&#2547; @(Convert.ToDouble(@item.price) - (Convert.ToDouble(@item.price)*(Convert.ToDouble(@item.discount)/100)))</span> </p>
                            


                            <button type="submit" class="btnn2" name="enter_search" style="width: 100%;">Details</button>
                            <input type="hidden" name="_id" value="@item.id">
                        </div>
                    </div>


                </form>

            </div>
        }
    </div>
</div>

